# Placeholder values -- DO NOT commit real secrets here. Generate your own
# and apply with `kubectl create secret generic smspit-secrets --from-literal=...`
# or a secrets manager (see checklist.md Day 87), instead of editing this
# file in place, if this namespace holds anything real.
apiVersion: v1
kind: Secret
metadata:
  name: smspit-secrets
  namespace: smspit
type: Opaque
stringData:
  POSTGRES_PASSWORD: changeme
  # sms-service requires an API key (Day 35) -- the dashboard needs one to
  # authenticate. Generate via POST /api/api-keys on auth-service, then set
  # it here (see docker-compose.yml's DASHBOARD_API_KEY for the same need
  # in the Compose flow).
  DASHBOARD_API_KEY: ""
