# Placeholder values in values.yaml -- override via -f my-secrets.yaml
# (gitignored) or --set, never commit real values. See README.md.
apiVersion: v1
kind: Secret
metadata:
  name: smspit-secrets
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "smspit.labels" . | nindent 4 }}
type: Opaque
stringData:
  POSTGRES_PASSWORD: {{ .Values.secrets.postgresPassword | quote }}
  DASHBOARD_API_KEY: {{ .Values.secrets.dashboardApiKey | quote }}
