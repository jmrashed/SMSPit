# Ports
GATEWAY_PORT=8080
AUTH_SERVICE_PORT=8000
SMS_SERVICE_PORT=3000
AI_SERVICE_PORT=8001
DASHBOARD_PORT=5173
JAEGER_UI_PORT=16686

# PostgreSQL
POSTGRES_USER=smspit
POSTGRES_PASSWORD=smspit
POSTGRES_DB=smspit
POSTGRES_PORT=5432

# Redis
REDIS_PORT=6379

# sms-service requires an API key as of Day 35; generate one via
# POST /api/api-keys on auth-service and set it here so the dashboard
# container can authenticate (see docker-compose.yml).
DASHBOARD_API_KEY=
