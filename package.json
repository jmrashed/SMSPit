{
  "name": "smspit-docs",
  "private": true,
  "description": "VitePress documentation portal for SMSPit, deployed to Vercel from the docs branch.",
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs"
  },
  "devDependencies": {
    "vitepress": "^1.5.0"
  }
}
