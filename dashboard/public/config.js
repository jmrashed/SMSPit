// Placeholder for local dev (`npm run dev` / `vite build`). The
// Docker image overwrites this at container startup with real values
// from the environment -- see docker-entrypoint.sh.
window.__APP_CONFIG__ = {};
